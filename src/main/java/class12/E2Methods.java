package class12;

public class E2Methods {

    int add(int num1, int num2){    // we remove void and replace int
                                     // because we have in type value
        int result=num1+num2;          // it should have result and result

        return result;
    }
         // create a method which takes a number multiply it with 10
         // returns the result.

          int multiply(int num) {      // keyword takes means parameter , multiply= method
        int res=num*10;
        return res;
          }

}